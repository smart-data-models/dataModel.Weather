/* (Beta) Export of data model WeatherForecast of the subject dataModel.Weather for a postgreSQL database. Pending translation of enumerations and multityped attributes */
CREATE TYPE WeatherForecast_type AS ENUM ('WeatherForecast');
CREATE TABLE WeatherForecast (address json, alternateName text, areaServed text, atmosphericPressure text, dataProvider text, dateCreated timestamp, dateIssued timestamp, dateModified timestamp, dateRetrieved timestamp, dayMaximum json, dayMinimum json, description text, feelLikesTemperature text, gustSpeed text, id text, illuminance text, location json, name text, owner json, precipitation text, refPointOfInterest text, relativeHumidity text, seeAlso json, source text, temperature text, type WeatherForecast_type, uVIndexMax text, validFrom timestamp, validTo timestamp, validity text, visibility json, weatherType text, windDirection text, windSpeed text);